#include <stdio.h>
#include <stdlib.h>

int linhas = 5;
int colunas = 10;
int tamanho**;

int main(){
  tamanho = malloc(sizeof(int*) * linhas);
	for(int i = 0; i < 5; i++){
		tamanho [i] = malloc(sizeof(int) * (colunas + 1));
	}

}


