<nome da tag>  -> é uma tag HTML.

<h1> = título nivel 1.

<h2> = título nivel 2.

<h3> = título nivel 3.

<h4> = título nivel 4.

<h5> = título nivel 5.

<h6> = título nivel 6.

<p> = paragráfo.

barra termina uma tag. => / .

<strong> = coloca texto em negrito. Dá importância ao conteúdo.

<em> = coloca texto em italico. Dá enfase ao conteúdo.

<p style=font-size: tamanho fonte> = Altera o tamanho da fonte.

<p style=text-align: posição> = Altera o tamanho da fonte.

P.S. => adição de duas ou + propriedades com ;  .

    A mexer na apresentação dos textos
        No alinhamento deles (text-align)
        No tamanho da fonte (font-size)
        Na cor de fundo (background)
        Na cor do texto (color)
    CSS inline
        Na linha onde temos a nossa tag, adicionamos a propriedade do CSS
    A tag <style>
        Dentro da tag, podemos colocar marcações de CSS referentes aos elementos que temos no nosso HTML
    A apresentação do CSS com um arquivo externo
    Como funciona o estilo em cascata do CSS
    Como importar um arquivo externo de CSS dentro da nossa página HTML
    Como representar cores no CSS
        Através do nome da cor
        Através do seu hexadecimal
        Através do seu RGB

id="nome" => identificação de um objeto.

height = altura.

width = largura.

margin = espaçamento  externo.

padding = espaçamento interno.

UX + UI + Desenvolvedor => Time de Front-End

<ul> = lista sem ordem.

<ol> = lista com ordem.

<li> = item da lista.

<div> = divisão de página

    A trabalhar com listas não-ordenadas e listas ordenadas
        Para cada um dos itens da lista, utilizamos a tag <li>
    O conceito das classes no CSS
        Elas servem para marcar itens, só que são repetíveis
    Como referenciar uma classe no CSS
    Divisões de conteúdo, utilizando a tag <div>
    Os comportamentos inline e block

<header> = cabeçalho da página.

__Tags_Estruturais__

<!DOCTYPE html> = mostra qual a versão do HTML a ser usada. 

P.S. => é uma tag de informação não precisa ser fechada.

<html> = renderiza todo o conteúdo que é HTML.

<meta charset="UTF-8"> = seleciona o dicionário de caracteres a serem usados.

<html lang = "pt-br"> = define a linguagem da página.

<title> = define o título da aba e da página.

<head> = cabeça da página. passar informações ao browser.

<body> = corpo da página. mostra o conteúdo no browser.

<img src=arquivo> = adiciona uma imagem.

__Propriedades_CSS__

CSS In Line;

CSS = ações em cascata.

<style> = propriedades de estilos às páginas.

tag {} = dar propriedades a todos que possuem a tag.

<link> = ligação a um arquivo externo.

	rel= -> relação do arquivo.

	href= -> local do arquivo.

#nome do id => referenciar no CSS.

class = classes.

__Cores__

rgb(R, G, B) => 0 até 255 cores.

hexadecimal = # ------ => 2 dígitos para o vermelho, o verde e o azul.